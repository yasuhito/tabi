#!/usr/bin/env ruby

$LOAD_PATH.unshift File.dirname( __FILE__ )
require "config"
require "rake"

sh "/sbin/ifconfig #{ ARGV[ 0 ] } 0.0.0.0 down"
sh "#{ $ovs_vsctl } del-port #{ $switch[ File.extname( __FILE__ )[ 1..-1 ].to_sym ][ :bridge ] } #{ ARGV[ 0 ] }"
